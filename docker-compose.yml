version: "3.4"
services:
  object-detection-streamlit:
    tty: true
    image: "eduardomatoss/object-detection-streamlit"
    stdin_open: true
    build:
      context: .
      target: "development"
    volumes:
      - ".:/app"
    networks:
      - db_nw

networks:
  db_nw:
    driver: bridge